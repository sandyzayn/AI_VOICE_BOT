apiVersion: v1
kind: Service
metadata:
  name: voice-app-service
spec:
  selector:
    app: voice-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80          # ← Change to 80 to match nginx port
  type: LoadBalancer